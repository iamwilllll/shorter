import { AppLayout } from '@/layout';
import { CreateUrlForm } from '@/components';

export function Dashboard() {
    return (
        <AppLayout>
            <CreateUrlForm />
        </AppLayout>
    );
}
